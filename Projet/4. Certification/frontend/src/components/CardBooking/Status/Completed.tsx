import { Typography } from '@mui/material';

export default function Completed({ booking, setBooking, type }: any) {
    return (
        <>
            <Typography>This booking is completed</Typography>
        </>
    );
}
